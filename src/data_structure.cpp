#include <iostream>
#include <string>
#include <vector>
#include <algorithm>

using namespace std;

// #ifndef F
// #define F first
// #define S second
// #define PB push_back
// #define MP make_pair
// #endif

int main(int argc, char const *argv[])
{
	// ios::sync_with_stdio(0);
	// cin.tie(0);

	// string s;
	// getline(cin, s)

	// freopen("input.txt", "r", stdin);
	// freopen("output.txt", "w", stdout);

	// typedef long long ll;
	// typedef vector<int> v;
	// typedef pair<int, int> pi;
	// ll a = 12345555;
	// ll b = 333333;
	// std::cout << a*b << '\n';

	// v.PB(MP(y1, x1));
	// v.PB(MP(y2, x2));

	// string array[] = {"hello", "my", "nameis", "rabih"};
	// int size = sizeof(array)/sizeof(*array);

	// cout << sizeof(array) << '\n' << sizeof(*array);

	// vector<int> v = {4,2,5,3,5,8,3};

	// sort(v.begin(), v.end());

	// for(int i=0; i < v.size(); i++) {
	// 	cout << v[i] << "\n";
	// }

	vector<int> v(10, 5);
	cout << v << '\n';

	return 0;
}